特征重要性排序：
                  特征       重要性
      妊娠时间（周数）  0.218645
      EPDS  0.180147
      母亲年龄  0.175896
      HADS  0.162703
      CBTS  0.160507
      教育程度  0.089403
      婚姻状况  0.008894
      分娩方式  0.003805


（搭配混淆矩阵图）
模型对每类行为的预测能力
1. 中等型（实际类别）
正确预测（中等型→中等型）：24 例
错误预测：
误判为安静型：12 例
误判为矛盾型：8 例
说明：模型对 “中等型” 的识别能力一般，存在较多误判（尤其易与 “安静型” 混淆），需关注特征区分度（如中等型与安静型的母亲指标差异是否不足）。
2. 安静型（实际类别）
正确预测（安静型→安静型）：35 例
错误预测：
误判为中等型：6 例
误判为矛盾型：3 例
说明：模型对 “安静型” 的识别能力相对最好（正确数最多、误判少），安静型与其他类别的特征差异更易被模型捕捉。
3. 矛盾型（实际类别）
正确预测（矛盾型→矛盾型）：38 例
错误预测：
误判为中等型：3 例
误判为安静型：3 例
说明：模型对 “矛盾型” 的识别能力较好（正确数高），但仍有少量误判（与中等型、安静型的特征边界需优化）。